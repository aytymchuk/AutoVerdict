# AutoVerdikt

Monorepo for the AutoVerdikt application.

## Directory Structure
- `frontend/`: (empty, placeholder)
- `backend/`: (empty, placeholder)
- `landing/`: (empty, placeholder)
- `ascode/`: (empty, placeholder)
- `scripts/`: Local utility and environment orchestration scripts
  - `run-local.sh`: Docker-first orchestration script
  - `clean-all.sh`: Environment cleanup script

## Quickstart
Run the application locally:
```bash
make run
```

Clean the local environment:
```bash
make clean
```